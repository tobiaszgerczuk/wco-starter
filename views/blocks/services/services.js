export default class Services {
  static selector = '.block-services';

  constructor(element) {
    this.element = element;
    this.init();
  }

  init() {
    // Block-specific behavior goes here.
  }
}
