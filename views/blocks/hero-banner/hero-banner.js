export default class HeroBanner {
  static selector = '.block-hero-banner';

  constructor(element) {
    this.element = element;
    this.init();
  }

  init() {
    // Add block-specific behavior for the hero-banner block here.
  }
}

