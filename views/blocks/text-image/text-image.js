export default class TextImage {
  static selector = '.block-text-image';

  constructor(element) {
    this.element = element;
    this.init();
  }

  init() {
    // Block-specific behavior goes here.
  }
}
