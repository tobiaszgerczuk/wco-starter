export default class Testowy {
  static selector = '.block-testowy';

  constructor(element) {
    this.element = element;
    this.init();
  }

  init() {
    // Block-specific behavior goes here.
  }
}
