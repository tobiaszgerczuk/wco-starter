export default class TwoColumns {
  static selector = '.block-two-columns';

  constructor(element) {
    this.element = element;
    this.init();
  }

  init() {
    // Structural block, no frontend behavior by default.
  }
}
